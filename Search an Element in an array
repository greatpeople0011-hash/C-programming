#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int size;
    scanf("%d", &size);
    int arr[size];
    for(int i=0; i<size; i++){
        scanf("%d", &arr[i]);
    }
    int val, i, flag=0;
     scanf("%d", &val);
    for(i=0; i<size; i++){
        if(arr[i]==val){
            flag=1;
            break;
        }
    }
        if(flag == 1)
            printf("Element found at position %d", i+1);
        else
            printf("Element not found");
       return 0;
}
