#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int size;
    scanf("%d", &size);
    int stu[size];
    for(int i=0; i<size; i++){
        scanf("%d", &stu[i]);
    }
    for(int i=size-1; i>=0; i--){
        printf("%d ", stu[i]);
    }   
    return 0;
}
